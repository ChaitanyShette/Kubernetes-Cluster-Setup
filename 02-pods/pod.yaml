apiVersion: v1
kind: Pod

metadata:
  name: nginx-pod
  namespace: devops-learning
  labels:
    app: nginx

spec:
  containers:
    - name: nginx
      image: nginx:1.27-alpine

      ports:
        - containerPort: 80
